export const STAGES = Object.freeze({
  DEV: 'dev',
  BUILD: 'build',
  BUILD_FAST: 'buildFast',
  PREVIEW: 'preview',
})

export const ENV_KEYS = Object.freeze({
  SITE_URL: 'SITE_URL',
  SITE_BASE_PATH: 'SITE_BASE_PATH',
  CI: 'CI',
})

export const CACHE_DIR = '.cache'
