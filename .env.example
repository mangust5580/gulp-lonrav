# Copy to .env.local (or .env) and adjust as needed.

# Base site URL used for sitemap/SEO URLs (no trailing slash).
SITE_URL=https://example.com

# Optional base path if the site is served from a subdirectory (e.g. /landing).
# Use empty value for root.
SITE_BASE_PATH=

# Optional build profile override.
# Examples: basic, full
GULP_LONRAV_PROFILE=

# Optional: reports directory for CI lint reports.
REPORT_DIR=reports
