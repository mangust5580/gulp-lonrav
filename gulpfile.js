import {
  clean,
  build,
  buildFast,
  dev,
  preview,
  devCheck,
  previewCheck,
} from '#gulp/public/index.js'

export { clean, build, buildFast, preview, devCheck, previewCheck }
export default dev
